============
Installation
============

At the command line::

    $ easy_install openpa-organigrammi

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv openpa-organigrammi
    $ pip install openpa-organigrammi